---
import { getLocationText } from "@/config/site"

interface JobOpening {
  title: string;
  type?: string;
  responsibilities: string;
  buttonText?: string;
  buttonHref?: string;
}

interface Props {
  badge?: string;
  title: string;
  description?: string;
  jobs: JobOpening[];
  introTitle?: string;
  introDescription?: string;
}

const { badge, title, description, jobs, introTitle: rawIntroTitle, introDescription: rawIntroDescription } = Astro.props;

const introTitle = rawIntroTitle ? getLocationText(rawIntroTitle) : undefined;
const introDescription = rawIntroDescription ? getLocationText(rawIntroDescription) : undefined;
---

<section class="jo-section" id="openings" style="background-image: url('https://images.unsplash.com/photo-1615799998603-7c6270a45196?q=80&w=704&auto=format&fit=crop&ixlib=rb-4.1.0&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D'); background-size: cover; background-position: center; background-attachment: fixed;">
  <div class="absolute inset-0 bg-white/75"></div>
  <div class="container mx-auto px-6 md:px-12 lg:px-16 py-16 md:py-20 relative z-10">

    {/* Intro header */}
    {introTitle && (
      <div class="jo-intro" data-animate="fade-up">
        <div class="jo-eyebrow">
          <svg class="jo-fan-icon" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="1"/><path d="M12 2v4M12 18v4M4.93 4.93l2.83 2.83M16.24 16.24l2.83 2.83M2 12h4M18 12h4M4.93 19.07l2.83-2.83M16.24 7.76l2.83-2.83"/></svg>
          <span class="jo-eyebrow-text">Careers</span>
        </div>
        <h2 class="jo-intro-title">{introTitle}</h2>
        <div class="jo-divider"></div>
        {introDescription && <p class="jo-intro-desc">{introDescription}</p>}
      </div>
    )}

    {/* Openings subheader */}
    <div class="jo-subheader" data-animate="fade-up">
      <div class="jo-subheader-inner">
        <div class="jo-subheader-line"></div>
        <h3 class="jo-subheader-title">{title}</h3>
      </div>
      {description && <p class="jo-subheader-desc" set:html={description} />}
    </div>

    {/* Job cards */}
    <div class="jo-grid" data-animate-stagger>
      {jobs.map((job, i) => (
        <div class="jo-card group">
          <span class="jo-card-accent"></span>

          {/* Background number */}
          <div class="jo-card-bg-num" aria-hidden="true">
            {String(i + 1).padStart(2, '0')}
          </div>

          <div class="jo-card-inner">
            <h3 class="jo-card-title">{job.title}</h3>
            <span class="jo-card-type">{job.type || "Full-time position"}</span>

            <div class="jo-card-sep"></div>

            <p class="jo-card-resp">
              <span class="jo-card-resp-label">Responsibilities:</span> {job.responsibilities}
            </p>

            <a
              href={job.buttonHref || "/contact"}
              class="jo-btn"
            >
              {job.buttonText || "APPLY NOW"}
              <svg xmlns="http://www.w3.org/2000/svg" class="w-4 h-4" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5">
                <path d="M5 12h14M12 5l7 7-7 7"/>
              </svg>
            </a>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>

<style>
  .jo-section {
    background: var(--brand-tertiary);
    position: relative;
    overflow: hidden;
  }

  /* ─── Intro Header ─── */
  .jo-intro {
    text-align: center;
    margin-bottom: 3rem;
    max-width: 70%;
    margin-left: auto;
    margin-right: auto;
  }
  .jo-eyebrow {
    margin-bottom: 1.25rem;
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 0.75rem;
  }
  .jo-fan-icon {
    width: 24px;
    height: 24px;
    color: var(--brand-highlight);
    animation: spin 3s linear infinite;
    flex-shrink: 0;
  }
  @keyframes spin {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
  }
  .jo-eyebrow-text {
    font-size: 14px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.2em;
    color: var(--brand-highlight);
    display: inline-block;
    padding: 0;
    background: none;
  }
  .jo-intro-title {
    font-size: 2rem;
    font-weight: 800;
    text-transform: uppercase;
    letter-spacing: -0.01em;
    line-height: 1.1;
    color: var(--brand-primary);
    margin-bottom: 1rem;
  }
  @media (min-width: 768px) {
    .jo-intro-title { font-size: 45px; }
  }
  .jo-divider {
    display: none;
  }
  .jo-intro-desc {
    font-size: 1.125rem;
    line-height: 1.75;
    color: var(--brand-secondary);
  }

  /* ─── Subheader ─── */
  .jo-subheader {
    margin-bottom: 2.5rem;
    max-width: 100%;
    margin-left: auto;
    margin-right: auto;
    text-align: center;
  }
  .jo-subheader-inner {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.75rem;
    margin-bottom: 0.5rem;
  }
  .jo-subheader-line {
    display: none;
  }
  .jo-subheader-title {
    font-size: 24px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: var(--brand-primary);
  }
  .jo-subheader-desc {
    font-size: 0.9rem;
    line-height: 1.7;
    color: var(--brand-secondary);
    padding-left: calc(3px + 0.75rem);
  }

  /* ─── Grid ─── */
  .jo-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1.5rem;
    max-width: 100%;
    margin: 0 auto;
    width: 100%;
  }

  /* ─── Card ─── */
  .jo-card {
    position: relative;
    background: white;
    border: 1px solid rgba(0, 0, 0, 0.06);
    overflow: hidden;
    transition: all 0.35s ease;
  }
  .jo-card:hover {
    border-color: var(--brand-highlight);
    transform: translateY(-4px);
    box-shadow: 0 12px 32px rgba(0, 0, 0, 0.06);
  }

  .jo-card-accent {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 3px;
    background: var(--brand-highlight);
    transform: scaleX(0);
    transform-origin: left;
    transition: transform 0.35s ease;
  }
  .jo-card:hover .jo-card-accent {
    transform: scaleX(1);
  }

  .jo-card-bg-num {
    position: absolute;
    bottom: -0.25rem;
    right: 0.5rem;
    font-size: 5rem;
    font-weight: 800;
    line-height: 1;
    color: var(--brand-primary);
    opacity: 0.03;
    pointer-events: none;
    user-select: none;
  }

  .jo-card-inner {
    position: relative;
    padding: 2rem 1.5rem;
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    text-align: left;
    min-height: 280px;
  }

  .jo-card-title {
    font-size: 20px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.02em;
    color: var(--brand-primary);
    margin-bottom: 0.5rem;
    line-height: 1.25;
  }

  .jo-card-type {
    font-size: 0.95rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--brand-secondary);
  }

  .jo-card-sep {
    width: 3rem;
    height: 2px;
    background: var(--brand-highlight);
    margin: 1.5rem 0;
  }

  .jo-card-resp {
    font-size: 13px;
    line-height: 1.7;
    color: var(--brand-secondary);
    margin-bottom: auto;
    padding-bottom: 1.5rem;
  }
  .jo-card-resp-label {
    font-weight: 700;
    color: var(--brand-primary);
  }

  .jo-btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.625rem;
    padding: 0.85rem 2rem;
    background: var(--brand-primary);
    color: #fff;
    font-weight: 700;
    font-size: 15px;
    text-decoration: none;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    transition: all 0.3s ease;
    align-self: flex-start;
  }
  .jo-btn:hover {
    background: var(--brand-highlight);
    color: #ffffff;
    transform: translateX(4px);
  }
  .jo-btn svg {
    transition: transform 0.3s ease;
  }
  .jo-btn:hover svg {
    transform: translateX(4px);
  }

  /* ─── Responsive ─── */
  @media (max-width: 1024px) {
    .jo-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }
  @media (max-width: 640px) {
    .jo-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
